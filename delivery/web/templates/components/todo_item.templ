package components

import "github.com/hobord/poc-htmx-go-todolist/entities"

templ TodoItem(todo *entities.Todo) {
  <li class="todo-item">
    <input type="checkbox" id="{ todo.ID }" checked?={ false } />
    <label for="{ todo.ID }">{ todo.Title }</label>
  </li>
}
